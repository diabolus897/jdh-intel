# 抓取剧本（PLAYBOOK.md）

> 每日情报抓取的**可复用执行剧本**。固化"怎么派子agent、每个agent带什么prompt、抓完怎么自检"，避免每轮重新设计。
> 配套：`AGENTS.md`(部门口径) + `SOURCES.md`(信源地图) + `validate.py`(自检脚本)。
> 最后更新：2026-06-25（**降本增效：默认改为「增量滚动池」模式**，全量降级为兜底）

---

## 〇、两种模式（先选模式，默认走增量）

> **核心理念升级（2026-06-25）**：`data.json` 不是"每天从零重抓的快照"，而是"**持续滚动维护的池子**"。25 号的全量结果就是基础池。每天只补"近 2 天的新料"，老料继承自昨天、自动按时效淘汰。
> **为什么**：30 天窗口里的料绝大部分昨天已抓过、今天仍有效，重抓=重复劳动，是 token 高、耗时 1.5h 的根因。增量化把每天的抓取量砍到约 1/5～1/10，质量不降反升（老料是已核验存量）。

| 模式 | 何时用 | 抓取窗口 | 起点 |
|---|---|---|---|
| **🟢 增量滚动（默认，每天跑）** | 日常每日更新 | **近 2 天**（今天+昨天）；漏跑则=距上次成功跑天数+1天 buffer | 继承昨天的 data.json（基础池） |
| **🔵 全量重抓（兜底）** | 首次建池 / 长期(>1周)没跑 / 口径大改 / 怀疑池子脏了 | 30/7 天分级（旧口径） | 从零 |

**增量模式 SOP（默认）**：
```
1. 读 AGENTS.md + PROJECT_STATUS.md + SOURCES.md（带上下文）
2. 拿上一版 data.json 作为「基础池」起点（archive/ 里最新一天，或当前根 data.json）
3. 算抓取窗口：近 2 天；若距上次成功跑 N 天，则窗口 = N+1 天
4. 按「子agent切分策略」并行派抓取agent，**prompt 窗口改为该增量窗口**（只要新料，别再抓 30 天）
5. 汇总新料 → 与基础池去重（url 同→丢；同公司同事换媒体→留信息全的）→ hi/mid 逐条 fetch 核日期
6. merge 进池：新料并入对应维度 → 滚动淘汰超时效的旧料（竞对/政策/财报 30 天，新品 7 天）→ 维度内按日期倒序
7. 更新 generated=今天；（可选）给本轮新增条目标记 firstSeen=今天，供前端"今日新增"高亮
8. python3 validate.py 自检全过
9. python3 linkcheck.py：只重点查本轮新增/改动条目的链接（老料上轮已验过），☠️死链必改/删
10. python3 archive.py 归档 + 重建 manifest
11. 更新 PROJECT_STATUS.md 状态表 + SOURCES.md 实测标记
12. git commit + push（含 archive/ 与 manifest.json）→ Netlify 自动部署
13. 出自检报告：本轮新增 N 条、淘汰 M 条超期、各维度现存条数、空维度说明、失败信源
```

**增量模式的关键纪律**：
- **某维度今日无新料 = 正常，明说"今日无新动态"**，下面接列池子里近期几条。**绝不为了填空去注水或扩窗口硬抓。**
- 去重底库 = `archive/data-*.json`（近 7 天足够，更早的本就超时效）。**不上向量库**，74条/天用不着。
- 淘汰只按时效铁律自动剔除，不手工纠结。被淘汰的旧料仍留在 archive 历史里，可追溯。
- 老料的 url 上一轮已 linkcheck 过，**本轮 linkcheck 聚焦新增/改动条目即可**，省时间。

---

## 一、整体流程（🔵 全量兜底模式，仅首次建池/口径大改/长期没跑时用）

```
1. 读 AGENTS.md + PROJECT_STATUS.md + SOURCES.md（带上下文）
2. 决定本轮范围：单部门重抓 / 全量6部门刷新
3. 按「子agent切分策略」并行派抓取agent，每个带全套铁律prompt（窗口 30/7 天分级）
4. 汇总 → 跨源去重 → 对照近7天历史去重（见下）→ 按时效铁律剔除超期 → 按维度归类
5. hi/mid 重点条目逐条 fetch 原文核确切日期，消除"待核"
6. 用 Python 写入 data.json（覆盖对应部门），不手改JSON避免格式错
7. python3 validate.py 自检，必须全过（看结尾「📋 修正清单」批量改错）
8. python3 linkcheck.py 死链检测：☠️ 死链必须改/删，⚠️ 待核逐条 fetch 确认
9. 本地 preview 渲染确认（可选但推荐）
10. python3 archive.py 归档：生成 archive/data-<日期>.json + 重建 manifest.json
11. 更新 PROJECT_STATUS.md 第5节状态表 + SOURCES.md 实测标记
12. git commit + push（含 archive/ 与 manifest.json）→ Netlify 自动部署（约1分钟）
13. 出自检报告：各维度条数、空维度原因、剔除/待核项、失败信源
```

---

## 二、子 agent 切分策略

> ⚠️ **铁律:抓取子 agent 只负责"联网抓取 + 结构化返回数据给主控",绝不允许自行写 data.json、跑脚本、git add/commit/push。** 汇总/去重/写入/校验/归档/提交一律由主控统一做。
> （2026-06-25 踩坑:某 consumer 子 agent 自作主张改了 data.json 并 push,只更了单部门、还归档了半成品。每个抓取 prompt 结尾必须写明"不要写入任何文件、不要提交,结构化文本返回"。）

**经验最优切分**（consumer 那轮验证，6并行≈5分钟，互不阻塞）：

| 切法 | 适用 |
|---|---|
| **按品类深抓 ×N** | 该部门有几个 GMV 大品类，每个 1 个 agent 深抓全维度 |
| **跨品类竞对 ×1** | 平台级竞对动作（大促战报/类目策略），跨所有品类一个 agent |
| **跨品类政策 ×1** | NMPA/医保/市监 监管动态，跨所有品类一个 agent |
| **补充品类 ×1** | 料稀薄的小品类合并到 1 个 agent，抓到几条算几条 |

**火力分配原则**：料密集的维度（竞对/政策）多分 agent；料稀薄的品类（如康复理疗）合并少分。不在低产维度浪费算力。

**卡片化部门的专属切法（即时零售 + 医药竞对）**：这两个部门竞对是固定玩家、走 layers 卡片结构，竞对动态单独派 agent 按"固定卡片"抓：
| 部门 | 卡片化抓取 agent |
|---|---|
| **即时零售** | ①O2O 平台 ×1：美团买药 + 淘宝闪购买药 近30天核心动态　②连锁药店 ×1：老百姓/益丰/大参林 动态 + 季度财报数字核实 |
| **医药** | 竞对 ×1：阿里健康动态（美团买药卡**复用即时零售的同一批数据，不重复抓**）|
详见第六节。

---

## 三、子 agent 的 prompt 模板

> 派每个抓取子 agent 时，用对应模板打底，再替换【】里的部门/品类/信源。

### 3.1 🟢 增量模板（默认，每天用）—— 只抓近 2 天新料

```
你是京东健康【部门名】部门的竞争情报分析师。今天是【YYYY-MM-DD】。
请联网抓取【品类/维度】**近 2 天（约【2天前日期】之后）新发生**的真实、权威情报。
这是每日增量更新：我们已有历史情报池，你只负责找"最新冒出来的新料"，不要回溯老新闻。

## 铁律（必须严格遵守）
1. 只报本次真实抓取到、有可点开 url 的内容；绝不编造事件/数字/公司名/产品名/url。
   url 必须是抓取到的真实原始链接。
2. **只要近 2 天的新料**（约【2天前日期】之后发布/发生）。更早的一律不要，主控池子里已有。
   唯一例外：近 2 天才首次披露的重大财报/政策/BD，即使事件本身稍早也收。
3. 日期：抓到精确日期填 "YYYY-MM-DD"，抓不到填 "待核" 不猜；重点条目再 fetch 原文确认。
4. 严格去重：同一公司同一件事只留信息最全的一条。
5. 新品维度只收"具体新品/新型号/新成分/新获批"；趋势/盘点/榜单/展会预告/选购指南一律不收。
6. 反软文：丢弃"十大/测评/排行/推荐"类营销内容。
7. **近 2 天确实没有合格新料的维度，直接说"今日无新料"——这是正常的，绝不为凑数放旧闻或软文。**

## 抓取范围
【品牌方清单 / 新品 / 竞对平台 / 政策面，参考 SOURCES.md 对应品类】
优先直连 SOURCES.md「一·五」的 RSS/列表页（FierceBiotech/动脉网/NMPA要闻/36氪等），增量场景下最快最准。

## 工具经验（来自 SOURCES.md）
- web_search 把"2026年X月"判成未来而拒答时，去年份改"近期/最新/今日"重试。
- web_search 只返回泛化话术时，改用 WebFetch 抓 html.duckduckgo.com/html/?q= 检索页。
- 品牌官网新闻页/Bing 常是 JS 空壳，换垂直媒体或官方公告；抓不到正文用 r.jina.ai/ 兜底。

## 输出格式
按维度返回近 2 天的真实新条目，每条含：维度、title、rel(hi/mid/lo)、summary(≤60字)、
source、date、url(真实)、takeaway(对JDH采销/商分的一句具体动作提示)。
没有新料的维度明说"今日无新料"。报告实际抓了哪些源、哪些失败。不编造凑数。
**只返回结构化文本给主控,不要写入任何文件、不要 git 提交。**
```

### 3.2 🔵 全量模板（兜底，首次建池/长期没跑/口径大改时用）

```
你是京东健康【部门名】部门的竞争情报分析师。今天是【YYYY-MM-DD】。
请联网抓取【品类/维度】的真实、近期、权威情报。

## 铁律（必须严格遵守）
1. 只报本次真实抓取到、有可点开 url 的内容；绝不编造事件/数字/公司名/产品名/url。
   url 必须是抓取到的真实原始链接。
2. 时效分级：竞对/财报/重大BD收购/政策 放宽近30天（约【30天前日期】之后）；
   新品/普通行业动态 守近7天（约【7天前日期】之后）。超期旧闻直接丢弃，不要保留。
3. 日期：抓到精确日期填 "YYYY-MM-DD"，抓不到填 "待核" 不猜；重点条目再 fetch 原文确认。
4. 严格去重：同一公司同一件事只留信息最全的一条。
5. 新品维度只收"具体新品/新型号/新成分/新获批"；趋势/盘点/榜单/展会预告/选购指南一律不收。
6. 反软文：丢弃"十大/测评/排行/推荐"类营销内容。

## 抓取范围
【品牌方清单 / 新品 / 竞对平台 / 政策面，参考 SOURCES.md 对应品类】

## 工具经验（来自 SOURCES.md）
- web_search 把"2026年X月"判成未来而拒答时，去年份改"近期/最新/618"重试。
- web_search 只返回泛化话术时，改用 WebFetch 抓 html.duckduckgo.com/html/?q= 检索页。
- 品牌官网新闻页/Bing 常是 JS 空壳，换垂直媒体或官方公告。

## 输出格式
按维度返回真实条目，每条含：维度、title、rel(hi/mid/lo)、summary(≤60字)、
source、date、url(真实)、takeaway(对JDH采销/商分的一句具体动作提示)。
没抓到合格内容的维度明说"无合格内容"。报告实际抓了哪些源、哪些失败。不编造凑数。
**只返回结构化文本给主控,不要写入任何文件、不要 git 提交。**
```

---

## 四、写入 data.json 的注意事项

- **用 Python 脚本写，不手改 JSON**：避免逗号/引号/超长 summary 等低级错误。
- summary **必须 ≤60 字**（validate.py 会卡），写之前自己数一下。
- 维度顺序：前5部门「竞对/政策/工业/新品」；**instant 是「竞对/政策/新技术·新模式/行业·资本」**。
- 维度内 items 按 date 从新到旧；无料维度 `items: []`。
- **卡片化部门（instant 全竞对 / 医药竞对）走 layers 结构**：卡内 `items` 也要按 date 倒序（validate.py 的 check_layers 会卡）。美团买药卡在 instant 和 pharma 两处 items 必须一致（复制同一份）。
- musts 跨维度挑最重要 3 条（优先 hi）。
- 改完务必 `python3 validate.py` 全过再提交。

---

## 五、卡片化部门专项抓取（即时零售 + 医药竞对）

这两个部门竞对走 layers 卡片结构，固定追踪几个对手，抓取方式与条目流不同。

### 6.1 即时零售 · O2O 平台 agent（美团买药 / 淘宝闪购买药）
prompt 要点（在标准模板基础上加）：
```
抓「美团买药」「淘宝闪购买药」近30天(约5月25日之后)核心动态，每个平台 2-3 条：
订单量/GMV份额、即时零售战略、扩品类、补贴、医药O2O整合、组织调整等。
**不放京东自家。** 每条带真实 url、date、source。按平台分组返回。
```
写入：instant 竞对 section 的 layers[0]（type=o2o）两张卡的 items。

### 6.2 即时零售 · 连锁药店 agent（老百姓 / 益丰 / 大参林）
prompt 要点：
```
抓「老百姓(603883.SH)」「益丰药房(603939.SH)」「大参林(603233.SH)」近30天动态(各2-3条)：
高管变动/并购/门店扩张/O2O布局/无人配送/股东会等。
**另核实每家「经营速览卡」(同口径,2025年报 + 最新季度两期)**：
  · 覆盖省份/优势省份
  · 营收(+同比)、归母净利(+同比)、毛利率、净利率、经营现金流(+同比)
  · 门店数(+增量)、直营(+增量)、加盟(+增量)
  · 战略要点 2-3 条(取自年报「管理层讨论与分析」未来战略/举措)
  · 投资者交流/业绩说明会纪要(标题+月份+url,如老百姓2025年报暨2026一季报解读会纪要)
来源用巨潮资讯(投资者关系/纪要)/公司IR/财报报道，**逐项带 url，查不到不写该字段、不编造**。
**取数路径见 SOURCES.md「二·五、上市公司财报&投资者纪要获取手册」**：财务数字走东财F10(`emweb.eastmoney.com/PC_HSF10/...code=SH603883`)、纪要走巨潮全文检索(`cninfo.com.cn/new/fulltextSearch?keyWord=`)，均用 `r.jina.ai/` 兜底。
**注意：老百姓正确股票代码是 603883.SH，不是 002424(那是贵州百灵)。**
```
写入：instant 连锁 layers[1]（type=chain）三张卡的 financials（coverage/periods/strategy/memo）+ items。
**经营速览卡只在季度披露后更新**（财务/覆盖/战略/纪要随财报季变）；平时这个 agent 只更新动态流 items。

### 6.3 医药 · 竞对 agent（阿里健康）
prompt 要点：
```
抓「阿里健康」近30天医药业务动态 1-2 条：B2C医药电商/O2O整合/财报/战略合作/自营药房/处方药。
```
写入：pharma 竞对 layers[0] 的阿里健康卡 items。
**美团买药卡直接复制 instant 部门美团卡的 items，不重复抓。**

### 6.4 写入 layers 的脚本提示
用 Python 改 data.json 时，竞对 section 结构是 `{"dim":"竞对动态","layers":[{type,title,cards:[{name,tag,code?,financials?,items}]}]}`，
不是 `items`。改完务必 `python3 validate.py`（check_layers 会校验卡片结构、财报 periods.label、卡内日期倒序）。

---

## 六、提交

```bash
# git 身份（本仓库）
git config user.name "情报官"; git config user.email "intel@jdh.local"
git add -A
git commit -m "情报更新 YYYY-MM-DD（说明本轮范围）"
git push   # Netlify 自动部署，约1分钟生效
```

---

## 七、跨期去重（对照近7天历史，防旧闻重报）

**痛点**：同一件事隔几天换个媒体重发，容易被当成新料再报，业务方看重复。

**轻量做法（不上向量库，57条/天用不着）**：写入前把今日候选与近 7 天历史比一遍——
- **底库**：历史归档 `archive/data-*.json`（归档功能上线后即天然去重库；上线前用上一版 data.json）。
- **判重**：
  - url 完全相同 → 直接丢（同一篇）。
  - 标题/主体高度重合（同公司同一件事，仅措辞/媒体不同）→ 人工判，留信息最全的一条，旧的丢。
- 只看近 7 天即可（更早的本就超时效）。这本是去重时的固有判断，历史库只是给一份"过去报过什么"的速查锚点。

---

## 八、三个收尾脚本分工（写完 data.json 必跑）

| 脚本 | 查什么 | 特性 | 失败处理 |
|---|---|---|---|
| `python3 validate.py` | **结构契约**：6部门顺序、dims、字段齐全、summary≤60、日期倒序、卡片结构 | 零依赖、秒级、**必须全过** | 看结尾「📋 修正清单」(如 `summary 超长×2　缺字段×1`) 批量改 |
| `python3 linkcheck.py` | **链接真实性**：所有 url 真实请求一次，防"看着真、点开404" | 需 requests、慢(并发)、网络相关 | ☠️ **死链**(404/410/DNS失败)必改或删；⚠️ **待核**(403/5xx/超时，多为反爬)逐条 fetch 确认，**别误删真链接** |
| `python3 archive.py` | **每日归档**：data.json→archive/data-<日期>.json，重建 manifest.json（前端日期选择器数据源） | 零依赖、秒级 | generated 非法会拒绝归档；正常无需干预。**在 validate+linkcheck 全过后、push 前跑** |

> validate / linkcheck **故意拆开**：前者是"必过的快校验"，后者是"慢的网络体检"，混在一起会让结构校验因网络抖动变得不稳定。
> linkcheck 参数：`--timeout 8 --workers 10`；`--strict` 把待核也算失败（一般不用）。
> archive 读 data.json 的 `generated` 当日期；根 data.json 始终=最新一天（前端默认&兜底都读它），历史在 archive/。归档文件**必须随 push 进 git**，Netlify 才能部署出历史日。
